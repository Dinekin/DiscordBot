<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Panel bota Discord - Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/dark-mode.css">
  <script src="/js/saved-style.js"></script>
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container mt-4">
    <div class="row">
      <div class="col-12">
        <div class="card shadow-sm">
          <div class="card-body">
            <h2 class="card-title">Twoje serwery</h2>
            <div class="alert alert-info mt-4">
              <i class="fas fa-info-circle me-2"></i>
              <% if (process.env.ALLOWED_GUILD_IDS && process.env.ALLOWED_GUILD_IDS.split(',').length > 0) { %>
                <strong>Uwaga:</strong> Ten bot jest dostępny tylko dla autoryzowanych serwerów Discord.
                <% if (process.env.BOT_OWNER_CONTACT) { %>
                  Skontaktuj się z właścicielem bota: <%= process.env.BOT_OWNER_CONTACT %> aby uzyskać autoryzację.
                <% } %>
              <% } %>
            </div>
                        <div class="row mt-4">
            <!-- Dodaj to na początku sekcji z serwerami, po <div class="row mt-4"> -->
              <% if (!hasAuthorizedGuilds && ownerContact) { %>
                <div class="col-12 mb-4">
                  <div class="alert alert-warning">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <strong>Uwaga:</strong> Nie posiadasz dostępu do żadnych autoryzowanych serwerów.
                    Skontaktuj się z właścicielem bota: <%= ownerContact %> aby uzyskać autoryzację.
                  </div>
                </div>
              <% } %>
              
              <!-- Zmodyfikuj przycisk dodawania bota w pętli foreach -->
              <% if (guild.hasBot) { %>
                <a href="/dashboard/guild/<%= guild.id %>" class="btn btn-primary mt-auto">
                  Zarządzaj
                </a>
              <% } else { %>
                <% if (guild.isAuthorized) { %>
                  <a href="https://discord.com/api/oauth2/authorize?client_id=<%= process.env.CLIENT_ID %>&permissions=8&scope=bot%20applications.commands&guild_id=<%= guild.id %>" class="btn btn-outline-primary mt-auto">
                    Dodaj bota
                  </a>
                <% } else { %>
                  <button class="btn btn-outline-secondary mt-auto" disabled title="Ten serwer nie jest autoryzowany">
                    Serwer nieautoryzowany
                  </button>
                <% } %>
              <% } %>
                                  </div>
                    </div>
                  </div>
                <% }); %>
              <% } else { %>
                <div class="col-12">
                  <div class="alert alert-info">
                    Nie masz dostępu do żadnych serwerów lub nie jesteś administratorem na żadnym serwerze.
                  </div>
                </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
  <script src="/js/dark-mode.js"></script>
</body>
</html>
